@page "/product"
@using Carnesia.Shared.CMS.Products.CreateProduct
@using Carnesia.Shared.CMS.Products.ProductList

<MudPaper Elevation="0">
    <MudContainer Class="mt-5 d-flex justify-center">
        <MudButtonGroup Color="Color.Primary" Variant="Variant.Outlined" OverrideStyles="false">
            <MudButton Color="Color.Primary" Variant="CurrentPage == CreateProduct ? Variant.Filled : Variant.Outlined" OnClick="() => ChangePage(CreateProduct)">
                @CreateProduct
            </MudButton>
            <MudButton Color="Color.Primary" Variant="CurrentPage == ProductList ? Variant.Filled : Variant.Outlined" OnClick="() => ChangePage(ProductList)">
                @ProductList
            </MudButton>
        </MudButtonGroup>
    </MudContainer>
</MudPaper>

@if(CurrentPage == CreateProduct)
{
    <CreateProduct />
}
else if(CurrentPage == ProductList)
{
    <ProductList />
}

@code {
    // Pages
    private string CreateProduct = "Create Product";
    private string ProductList = "Product List";

    // Current Page
    private string CurrentPage = "Create Product";

    // Change Page
    private void ChangePage(string page)
    {
        CurrentPage = page;
    }
}
